[settings]
# Local lab only: the lesson cockpit embeds Splunk Web in an iframe through a
# local HTTP reverse proxy. Production Splunk Web should keep stricter cookie
# and frame settings.
verifyCookiesWorkDuringLogin = false
tools.sessions.secure = false
x_frame_options_sameorigin = false

